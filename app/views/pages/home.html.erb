<h1>All Lists</h1>
<% @lists.each do |list| %>
  <h2><%= list.name %></h2>
  <ul>
    <% list.movies.each do |movie| %>
      <li><%= movie.title %> - <%= movie.rating %></li>
    <% end %>
  </ul>
<% end %>
